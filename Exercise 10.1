def nested_sum(t):
    total = 0
    for x in t:
        if type(x) == list:
            total += nested_sum(x)
        else:
            total += x
    return total


num_list = [[1,2,[10, 10], 3], [4,5, 6], [7, 8], 9]
print(nested_sum(num_list))
